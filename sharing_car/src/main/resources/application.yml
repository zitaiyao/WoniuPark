server:
    port: 8080
spring:
    application:
        context-path: /
    datasource:
        type: com.zaxxer.hikari.HikariDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/k15?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
        username: root
        password: root

logging:
    level:
        com:
            woniu:
                dao:
                    mysql: debug